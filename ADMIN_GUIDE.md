# Руководство администратора Kraken Assistance

Версия документации: **1.914.0208**  
Бот: **Kraken Assistance#5235**  
Discord-сервер: **Kraken Famq**  
Majestic-сервер: **RU18 / Orlando**

## 1. Назначение бота

Kraken Assistance обслуживает основные внутренние процессы семьи:

- профили участников;
- ранги и повышения;
- баллы MP;
- дисциплинарные предупреждения;
- заявки в RP- и CAPT-составы;
- обычные обращения;
- блокировку доступа к функциям бота;
- массовое перемещение между голосовыми каналами;
- автоматические панели и централизованные логи.

Все рабочие данные находятся в облачной MySQL. Локальных JSON-баз у бота нет.

## 2. Уровни доступа

### Руководство семьи

Роль руководства задаётся в `config.json`, поле `leadershipRoleIds`.

Руководство может использовать:

- `/info`;
- `/move`;
- административную панель для профилей, варнов, рангов, баллов и составов.

### Рассмотрение обращений и заявок на вступление

И обычные обращения, и заявки на вступление доступны одному и тому же кругу лиц:

- пользователям с правом `Administrator`;
- любому участнику с ролью из `config.json → leadershipRoleIds` (High Staff и выше:
  High Staff, Administrator, Deputy, Owner).

Роль PR Assistant (4 ранг) отдельного доступа к заявкам больше не даёт — это обычный
ранг без специальных прав.

### Проверка автоматических начислений MP

Карточки проверки MP-отчётов отправляются в личные сообщения назначенному администратору с Discord ID `629552401237540874`.

## 3. Общие правила работы

1. Перед важными изменяющими действиями бот показывает личное подтверждение. Отправка анкеты на вступление и её принятие выполняются сразу, без повторного окна.
2. Нажатие «Отменить» просто удаляет окно подтверждения.
3. При подтверждении выполняется действие и отправляется лог.
4. Бот сохраняет рабочие данные в MySQL и перечитывает их для событий, которым требуется актуальное состояние. Большинство ручных изменений в базе не требует перезапуска.
5. Никогда не запускайте два экземпляра бота с одним токеном одновременно.
6. Не редактируйте ID каналов и ролей без одновременного изменения конфигурации.

## 4. Пользовательские команды

### Кнопка «Личный кабинет»

Кнопка находится в основной панели личного кабинета и показывает собственный профиль:

- Discord-пользователя;
- текущий ранг;
- число активных варнов;
- баланс MP;
- историю баллов;
- историю варнов;
- историю рангов;
- настройку личных уведомлений.

Профиль другого пользователя через эту кнопку открыть нельзя. Команда `/profile` удалена.


## 5. Ранги

### Раздел «Ранги» административной панели

Кнопка «Ранги» открывает действия «Повысить» и «Понизить». В форме можно указать до десяти Discord ID или упоминаний, а также обязательную причину.

После подтверждения бот:

1. проверяет, что роль бота находится выше всех затрагиваемых ранговых ролей;
2. выдаёт роль нового ранга и только после этого снимает прежнюю;
3. записывает изменение в `rank_logs`;
4. обновляет текущий ранг в `users`;
5. обнуляет баланс MP участника;
6. пишет лог;
7. отправляет участнику личное уведомление.

Роли рангов:

| Ранг | Discord Role ID | Отображение в профиле |
| --- | --- | --- |
| 1 | `1267727446451421235` | 1 |
| 2 | `1267721963627876384` | 2 |
| 3 | `1267721961572925472` | 3 |
| 4 | `1515822154396991488` | 4 |
| 5 | `1533210748656816310` | 5 |
| 6 | `1267721952613634089` | 6 |
| 7 | `1267721947496583253` | 7 |
| 8 | `1266010750699442206` (High Staff) + `1515813606917148797` (Administrator) | 8 |
| 9 | `1265996494922846219` | Deputy Leader |
| 10 | `1265995446975856691` | Leader |

Ранг 4 — обычный ранг без специальных прав (роль повышения/понижения через
админ-панель не участвует, назначается и снимается вручную по мере необходимости).

Ранг 8 — единственный ранг с двумя ролями: бот при автоматическом повышении с 7
ранга выдаёт только High Staff, роль Administrator назначается администрацией
вручную. Обе роли засчитываются как 8 ранг и обе снимаются ботом при уходе с
8 ранга.

Раздел «Повышение» команды `/info` показывает участников, которые набрали достаточно MP для следующего повышения:

- 1 → 2: фамилия Kraken;
- 2 → 3: 75 баллов;
- 3 → 5: 150 баллов;
- 5 → 6: 300 баллов;
- 6 → 7: 450 баллов;
- 7 → 8: 600 баллов.

Дополнительные требования — контракты, MP, взносы и разговоры — проверяются администрацией вручную. Девятый и десятый ранги выдаются только по решению основателей.

Команда `/info` открывает две кнопки: «Повышение» и «AFK». «Повышение» показывает кандидатов на следующий ранг, а «AFK» — учитываемых участников, которым MP не начислялись семь дней. Оба списка имеют постраничную навигацию.

## 6. Ручное управление MP

### Раздел «Баллы» административной панели

Кнопка «Баллы» открывает действия «Добавить» и «Списать». В форме указываются количество, обязательная причина и до десяти Discord ID или упоминаний.

Учёт MP доступен только участникам со 2-го по 7-й ранг. Первый ранг, участники без ранга и участники с 8-м рангом и выше пропускаются при ручном и автоматическом начислении; баланс и история MP у них не отображаются в профиле. Проверка выполняется по актуальным ранговым ролям Discord, поэтому отдельный список Discord ID не используется.

Количество поддерживает шаг 0,5. Баланс не уходит ниже нуля. Каждая операция:

- меняет баланс в `users`;
- сохраняется в `mp_point_transactions`;
- записывается в канал логов;
- отправляет личное уведомление участнику, если уведомления включены.

## 7. Автоматические отчёты MP

Бот обрабатывает сообщения в настроенном канале баллов MP `1315852973368152155`.

Сообщение должно содержать распознаваемый вид мероприятия и изображения. Поддерживаются:

| Вид | Баллов каждому |
| --- | ---: |
| Подставная стройка | 5 |
| Гровер | 15 |
| Ценная партия | 10 |
| Похитка | 10 |
| Конспирация I | 10 |
| Удачный захват территории | 15 |
| Неудачный захват территории | 5 |

Для обычных MP бот распознаёт отмеченные игровые имена на скриншотах и сопоставляет их с полным Discord-именем либо отдельным именем без фамилии. Для каптов бот читает табличный скриншот и определяет `WIN` или `LOSS`.

Участники вне диапазона 2–7 рангов автоматически исключаются из итоговых списков MP и не могут быть добавлены в начисление вручную.

При капте баллы получают только строки, в которых урон больше нуля. Число убийств не влияет на допуск.

### Проверка отчёта администратором

В личные сообщения приходит карточка со следующими кнопками:

- **Подтвердить** — начислить баллы найденным участникам;
- **Отклонить** — отказать в начислении;
- **Другие действия** — открыть личное меню, внутри которого доступны добавление участников, исключение участников и изменение вида MP.

После выбора нового вида MP бот показывает отдельное подтверждение. Название мероприятия и сумма сохраняются в MySQL.

После итогового решения на исходное сообщение ставится кастомная реакция:

- круглая галочка — отчёт принят;
- круглый крестик — отчёт отклонён.

## 8. Варны

### Раздел «Варны» административной панели

Кнопка «Варны» открывает действия «Добавить» и «Снять». В форме можно указать до десяти Discord ID или упоминаний и обязательную причину.

Роли варнов:

- 1/3 — `1290775235360194610`;
- 2/3 — `1290775323373211770`;

При достижении 3/3 бот отправляет уведомление пользователю и снимает с него все роли на
Discord-сервере (участник остаётся на сервере, исключения не происходит). Бан и
специальная роль 3/3 не выдаются. Все действия логируются.

## 9. Заявки на вступление

### Панель

Бот автоматически создаёт и обновляет панель вступления. В ней отдельно отображаются:

- набор в CAPT-состав;
- набор в RP-состав;
- статус каждого набора;
- условия подачи и повторной заявки.

Если оба состава закрыты, выбор анкеты недоступен.

### Раздел «Составы» административной панели

Кнопка «Составы» позволяет выбрать Capt, RP или оба состава. После выбора бот показывает текущее действие и запрашивает подтверждение переключения набора.

### RP-анкета

Содержит:

- IC имя / уровень персонажа / Static ID;
- OOC возраст;
- причину вступления.

### CAPT-анкета

Дополнительно содержит:

- выбор `Collers` или `Main`.

### Обработка заявки

В ветке доступны кнопки:

- **Принять**;
- **Отклонить**;
- **Передать**.

При принятии бот автоматически назначает нажавшего администратора ответственным, закрывает заявку и выдаёт:

- 1 ранг `1267727446451421235`;
- роль Verified `1265995505524015245` — одинаковая для RP- и CAPT-заявок.

При отклонении обязательно вводится причина. Причина сохраняется в логах и базе. Пользователь получает уведомление, что повторную заявку можно подать через 10 дней.

При передаче заявки нужно указать Discord ID или упоминание нового администратора. Если заявку ещё никто не взял, передача невозможна. Первоначальный ответственный удаляется из ветки, новый добавляется.

Заявка может автоматически браться администратором при его сообщении или реакции в ветке.

## 10. Обычные обращения

Панель «Система обращений» содержит:

- **Создать обращение**;
- **Готовые решения**.

При создании пользователь выбирает тип:

- заявка на начисление баллов;
- заявка на премию;
- заявка на повышение;
- заявка на отпуск;
- другое.

После выбора пользователь вводит детали. Обращение направляется администрации в отдельный закрытый раздел.

В обычном обращении доступны:

- **Закрыть** — сразу назначает администратора ответственным и завершает обращение;
- **Передать** — передаёт уже взятое обращение другому администратору.

Отдельных статусов «принято» и «отклонено» у обычных обращений нет. Конечный статус — «Закрыто».

Обращение можно взять сообщением или реакцией в ветке. При мгновенном закрытии отдельное сообщение о взятии не отправляется.

## 12. Голосовые каналы

### Команда `/move`

```text
/move from:<исходный голосовой канал> to:<целевой голосовой канал>
```

После подтверждения бот переносит всех участников. В ответе отображается количество успешных перемещений и список тех, кого переместить не удалось.

Боту нужны права:

- `View Channel`;
- `Connect`;
- `Move Members`.

Категории команда не изменяет.

## 13. Автоматические панели

Команды `/panels` нет. После запуска бот сам:

- находит существующие панели;
- обновляет их;
- создаёт недостающие;
- сохраняет необходимые ссылки и идентификаторы в MySQL.

Административная панель автоматически публикуется в канале
`1291543297747194010`. Она содержит разделы по варнам, рангам, баллам,
составам, AFK и общим правилам. Ответы кнопок видит только нажавший участник
руководства.

Если панель не обновилась:

1. проверьте доступ бота к каналу;
2. проверьте права `View Channel`, `Send Messages`, `Embed Links`, `Create Private Threads`, `Send Messages in Threads`, `Manage Threads`;
3. проверьте правильность ID канала;
4. перезапустите процесс и изучите PM2-логи.

## 14. Логи

Основной канал логов задаётся в `config.json` как `logChannelId`.

Логируются:

- ранги;
- начисления и списания MP;
- автоматические MP-отчёты;
- варны;
- создание и завершение заявок;
- принятие и отклонение заявок;
- передача ответственному;
- изменения набора;
- массовое перемещение в голосовых каналах.

Если лог не отправился, проверьте ID канала и права `View Channel`, `Send Messages`, `Embed Links`.

## 15. MySQL

Рабочие таблицы:

| Таблица | Назначение |
| --- | --- |
| `users` | Профили, текущий ранг, баланс MP, варны, уведомления |
| `recruitment_settings` | Отдельные строки Capt и RP со статусом и временем обновления |
| `tickets` | Вступление и обычные обращения |
| `rank_logs` | История рангов |
| `warning_logs` | История варнов |
| `mp_point_transactions` | Начисления, списания и заявки MP |
| `afk_sessions` | Активные игровые AFK-сессии и время их завершения |

Категории `tickets`:

- `application` — вступление;
- `support` — обычное обращение;

Соединение обязательно использует TLS и сертификат `ca_mysql.crt`.

Не удаляйте строки из активных заявок без понимания последствий. Бот перечитывает MySQL, поэтому ручное удаление может сделать существующую Discord-ветку неуправляемой.

Очистка `mp_point_transactions` удаляет историю операций и ожидающие проверки, но текущие балансы находятся в `users.mp_balance`. Перед ручной очисткой обязательно сделайте резервную копию.

## 16. Выход и бан участника

Когда пользователь выходит с Discord-сервера или получает бан, бот удаляет его ветки и сообщения заявок, а также все его пользовательские строки из `users`, `tickets`, `rank_logs`, `warning_logs`, `mp_point_transactions` и `afk_sessions`. Уже отправленные сообщения в Discord-канале логов сохраняются. Уведомления «Профиль пользователя создан» и «Профиль пользователя удалён» в логи не отправляются.

### Игровая AFK-панель

AFK-панель открывается кнопкой **«AFK-система»** в личном кабинете. Участник может уйти в AFK максимум на четыре часа за один раз или вернуться раньше; раннее возвращение требует личного подтверждения. Причина видна только в административных логах, а пользователю отображаются участники и время возвращения. Активная сессия хранится в `afk_sessions`, поэтому перезапуск бота её не сбрасывает. Каждые 30 секунд бот удаляет истёкшие сессии и отправляет участнику личное сообщение.

## 17. Установка и обновление на сервере

Требуется Node.js **20.9.0 или новее**.

Обязательные файлы:

- `src/index.js`;
- `src/storage.js`;
- `src/register-commands.js`;
- `src/application-emojis.json`;
- `config.json`;
- `package.json`;
- `package-lock.json`;
- `.env`;
- `ca_mysql.crt`.

Папка `assets` для обычной работы не нужна: она хранит исходники уже загруженных прикладных эмодзи.

Обновление:

```bash
cd /root/bot
npm install
npm run register
pm2 restart kraken-bot
pm2 logs kraken-bot --lines 100
```

Выход из просмотра логов: `Ctrl + C`.

## 18. Проверка после обновления

1. Выполните `/info` и проверьте оба списка.
2. Откройте кнопку «Личный кабинет» и кнопки истории.
3. Проверьте панели вступления и личного кабинета.
4. Создайте тестовое обращение и убедитесь, что доступ к нему есть только у заявителя и ответственной администрации.
5. Проверьте кнопку закрытия и лог.
6. Проверьте переключение одного состава в административной панели и верните нужный статус.
7. Проверьте PM2-логи на отсутствие `Unknown interaction`, `Missing Permissions`, ошибок TLS и MySQL.

## 19. Типовые ошибки

### `ENOENT: no such file or directory, open '/root/package.json'`

Команда выполнена не в папке бота:

```bash
cd /root/bot
```

### `Unknown interaction` / код Discord `10062`

Discord не получил первоначальный ответ вовремя. Проверьте задержку сети, нагрузку OCR и наличие повторной отправки ответа на одно взаимодействие.

### `Missing Permissions`

У бота нет нужного права или его роль находится ниже целевой роли. Проверьте права канала и иерархию ролей.

### Ошибка MySQL или TLS

Проверьте:

- значения `MYSQL_HOST`, `MYSQL_PORT`, `MYSQL_USER`, `MYSQL_PASSWORD`, `MYSQL_DBNAME`;
- наличие `ca_mysql.crt` в корне `/root/bot`;
- доступность IP сервера в настройках облачной базы;
- отсутствие лишних пробелов в `.env`.

### Панель продублировалась

Убедитесь, что одновременно работает только один экземпляр процесса:

```bash
pm2 list
```

### Команды не обновились

Повторно зарегистрируйте команды:

```bash
cd /root/bot
npm run register
pm2 restart kraken-bot
```

## 20. Безопасность

- Никому не отправляйте `.env` и токен Discord.
- Если токен попал на скриншот или был опубликован, немедленно сбросьте его в Discord Developer Portal.
- Не храните `.env` в публичном Git-репозитории.
- Не отключайте проверку TLS-сертификата MySQL.
- Перед массовыми изменениями базы создавайте резервную копию.
- Не выдавайте роли бота права выше необходимых.
